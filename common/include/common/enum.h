#pragma once

namespace flower::common
{
    enum Direction {
        LEFT,
        RIGHT,
        UP,
        DOWN,
        MOTIONLESS
    };

    enum SnakeState{
        READY,
        STAND
    };
}
